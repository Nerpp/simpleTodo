<form method="post" action="{{ path('app_task_delete_all', {'id': listing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listing.id) }}">
    <button class="btn btn-danger">Supprimer toutes les taches finies</button>
</form>