{% extends 'base.html.twig' %}

{% block title %}Edit Task
{% endblock %}

{% block body %}
	<div class="container">
		<div class="d-grid gap-3">
			<div class="text-center">
				<h1>Edition de {{ task.title }}</h1>
			</div>
			<div class="">
				{{ include('task/_form.html.twig') }}
			</div>
			<div class="d-flex flex-row justify-content-around">
				<div class="">
					<a href="{{ path('app_listing_show', {'id': task.listing.id}) }}" class="btn btn-primary">retour</a>
				</div>
				<div class="">
					{{ include('task/_delete_form.html.twig') }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
